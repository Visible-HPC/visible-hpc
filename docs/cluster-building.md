1. once the raspberry pi is connected to the tray, insert USB stick with image, plug it in and connect to keyboard and monitor
2. pull up command line with `ctl+alt+F1`
3. get serial number with `cat /proc/cpuinfo`. the serial number are the last eight digits
4. 
